import { useState } from "react";
import styled from "styled-components";

const HeaderElem = styled.header`
  width: 100vw;
  height: 8vh;
  background-color: #ccc;
`;

const Header = (props) => {
  return <HeaderElem></HeaderElem>;
};

export default Header;
